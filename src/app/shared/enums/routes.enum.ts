export enum RoutePath {
  EMPTY = '',
  SLASH = '/',
  WILDCARD = '**',
  PATH_MATCH_FULL = 'full',
  PATH_MATCH_PREFIX = 'prefix',
  HOME = 'home',
  LOGIN = 'login',
  FLOOR_PLAN = 'floor-plan',
  ID_PATH = ':id',
  ORIGIN_PATH = ':origin',
  ERROR = 'error-page'
}
